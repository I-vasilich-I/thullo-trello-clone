const API_ENDPOINTS = {
  ACTIVATE: 'api/activate',
};

const DEFAULT_CLIENT_URL = 'http://localhost:3000/';
const ACTIVATION_LINK_URL = `http://localhost:5000/${API_ENDPOINTS.ACTIVATE}`;

export { API_ENDPOINTS, DEFAULT_CLIENT_URL, ACTIVATION_LINK_URL };
